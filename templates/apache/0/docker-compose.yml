version: '2'
services:
  apache:
    image: php:apache
    volumes:
      - content:/var/www/html/
  apache-lb:
    image: rancher/lb-service-haproxy:latest
    ports:
      - ${PUBLISH_PORT}:80

volumes: 
  content:
    driver: ${VOLUME_DRIVER}
    external: true